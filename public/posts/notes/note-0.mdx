---
title: masOS 세팅
date: 2025-12-09T22:15:00
update: 2026-01-12T10:20:00
---
![[imgs/mac_setting/macmini.png]]

이번에 M4 맥미니를 질렀습니다. 끼얏호우
윈도우만 10년 넘게 쓰다가 맥os를 쓰려니 답답한게 많더군요. 이번에도 세팅한 것들을 잊어버리기전에 메모해두려고 글을 작성합니다.

(그런데 맥 세팅 방법.. 이라고 해야하나? 그냥 사자마자 했던 것들을 적었습니다.)

>macOS는 Tahoe 26.1로 업데이트한 상태입니다.

# 키보드 세팅
## command ctrl 키 변경
일단 윈도우의 `win`키가 없어지고 `command`키가 생겼는데 윈도우의 `ctrl` 역할을 많이 하더군요. 그래서 `command`키하고 `ctrl`키를 바꿔줬습니다.

/imglist open
![[imgs/mac_setting/mac_keyboard_setting_0.png]]
![[imgs/mac_setting/mac_keyboard_setting_1.png]]
/imglist close
시스템설정 - 키보드 - 키보드 단축키 - 보조 키
여기서 키를 바꿀 수 있습니다.

## 한/영 키
![[imgs/mac_setting/mac_keyboard_setting_2.png]]

`Caps Lock`으로 `한/영`을 바꾸는 것도 매우 불편했습니다. 그래서 입력 소스 전환 버튼을 비활성시켰습니다.

![[imgs/mac_setting/mac_keyboard_setting_3.png]]

`한/영`키는 `F18`로 바꿔놨습니다. 지금은 via를 지원하는 키보드를 사용하고 있어서 그냥 바꿀 수 있었지만 소프트웨어로 해결해야 한다면 `karabiner`를 써야할 것 같네요.

## 구름 입력기
![[imgs/mac_setting/hangul.png]]

글자를 치다보면 '맥' 이라는 글자가 'aㅐㄱ'으로 타이핑되는 경우가 많아서 참 답답했는데 [구름입력기](https://gureum.io/)란 것이 있더라고요. macOS용 한글 입력기인데 [libhangul](https://github.com/libhangul/libhangul) 기반으로 모아치기를 지원한다고 합니다. 가볍고 좋은 것 같습니다. 확실히 오타도 줄어들었고요.

## Home, End 키 세팅
맥에서 `Home` 키와 `End` 키를 누르면 문서의 첫 부분과 마지막으로 이동하더군요. 문장의 첫 부분과 끝 부분으로 이동하기를 원했기에 키바인딩 설정을 바꿨습니다.

```powershell
cd ~/Library
mkdir KeyBindings
cd KeyBindings
vi DefaultKeyBinding.dict
```

```c
{
    /* Remap Home / End keys to be correct */ 
    "\UF729" = "moveToBeginningOfLine:"; /* Home */ 
    "\UF72B" = "moveToEndOfLine:"; /* End */ 
    "$\UF729" = "moveToBeginningOfLineAndModifySelection:"; /* Shift + Home */ 
    "$\UF72B" = "moveToEndOfLineAndModifySelection:"; /* Shift + End */ 
    "^\UF729" = "moveToBeginningOfDocument:"; /* Ctrl + Home */ 
    "^\UF72B" = "moveToEndOfDocument:"; /* Ctrl + End */ 
    "$^\UF729" = "moveToBeginningOfDocumentAndModifySelection:"; /* Shift + Ctrl + Home */ 
    "$^\UF72B" = "moveToEndOfDocumentAndModifySelection:"; /* Shift + Ctrl + End */ 
}
```

`DefaultKeyBinding.dict` 파일에 위 내용을 넣어주고 재부팅하면 됩니다.

---

# 디스플레이 세팅
![[imgs/mac_setting/betterdisplaypro.png]]
>https://github.com/waydabber/BetterDisplay

FHD 모니터를 쓰고있는데 요상하게 글자가 흐릿하게 보여서 눈이 침침해지더군요. 시스템에서 폰트에 안티앨리어싱을 먹이고 있는 것 같은데 끄는 옵션이 보이지 않아서 찾은 앱입니다.

![[imgs/mac_setting/betterdisplaypro_hidpi.png]]

**HiDPI** 기능을 이용해서 좀 더 글자가 선명하게 보이게 만들었습니다. 해상도를 늘리고 4개의 픽셀을 이용해서 1개의 픽셀을 표현하는 것 같습니다.

글자는 선명해져서 좋은데 도트를 찍을 때는 선명했던 경계가 흐려지는 경향이 조금 보이네요. 화면에서 처리하는 픽셀 수가 늘어나서 그런 것 같습니다. 도트를 찍을 때는 끄고 코딩하거나 구글링 할 때는 켜는 식으로 사용하고 있습니다.

---

# git 설치 및 깃헙 연동
일단 [brew](https://brew.sh/ko/)를 통해서 설치할 것이므로 brew를 설치해줍시다.

```powershell
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

그 다음에 git을 설치하면 됩니다.

```powershell
brew install git
```

그 다음에 깃헙 연동을 할 것인데.. 저는 그냥 비주얼코드를 통해서 깃헙 로그인을 했습니다. 이전에 만들었던 비공개 repo를 clone하니까 알아서 브라우저가 열리면서 로그인 창으로 이동하더군요.

터미널로 깃헙 연동을 한다고 하면 토큰 발급을 해야한다고 합니다. 비밀번호 인증이 사라졌다고 하네요.

![[imgs/mac_setting/github_token_0.png]]

프로필 이미지 클릭 - Settings - Developer settings

![[imgs/mac_setting/github_token_1.png]]

그 다음 클래식 방법으로 토큰을 생성시킵니다.

![[imgs/mac_setting/github_token_2.png]]

마지막으로 어떤 용도로 사용할 것인지 체크한 다음에 토큰을 발급 받으면 됩니다. 발급된 토큰을 복사해서 비밀번호 대신 입력하면 인증이 완료됩니다.

---

# 터미널 꾸미기
기본 터미널도 이쁘긴한데 가독성을 좀 높이고 싶더군요. 좀 더 편하게 쓰고 싶기도 했고요.

## iTerm2
![[imgs/mac_setting/iterm2.png]]
>https://iterm2.com/

iTerm2를 설치했습니다. 써보니 창을 분할하거나 탭으로 관리할 수 있어서 좋더라고요. 자동 완성 기능도 제공해준다고 합니다.

## oh my zsh
![[imgs/mac_setting/ohmyzsh.png]]
>https://ohmyz.sh/

친구가 이쁜 테마를 쓰고 있길래 저도 설치해봤습니다. 이걸로 터미널 모양을 바꿀 수 있더라고요.

![[imgs/mac_setting/apply_powerlevel10k.png]]

저는 [powerlevel10k](https://github.com/romkatv/powerlevel10k)가 이뻐서 이걸 설치했습니다. 그리고 [iTerm2-Color-Schemes](https://iterm2colorschemes.com/)에서 'Ayu'라는 프리셋을 받아서 적용했습니다.

---

# 스팀 설치
이상하게 스팀 설치파일을 받고 돌리면 손상된 파일이라면서 휴지통에 던져버리더군요. GateKeeper가 보안에 위협을 줄 수 있다고 판단하면 파일/앱 실행을 막는다고 합니다.

```powershell
sudo xattr -r -d com.apple.quarantine /Users/[user_name]/Downloads/steam.dmg
```

파일/폴더의 Extended Attributes를 제거하는 명령어를 사용했습니다. 이렇게 하니 정상적으로 스팀 설치가 되더군요. 빵긋

---

# 알탭 지원
![[imgs/mac_setting/AltTab.png]]
>https://alt-tab-macos.netlify.app/

친구가 추천해준 앱입니다. 윈도우처럼 알탭을 쓸 수 있게 해줍니다. 그런데 켜져있는 모든 앱이 다 보이는 느낌이라 '**숨겨진 윈도우 표시 - 숨기기**' 해놓고 쓰고 있습니다. (`cmd`+`H`를 하면 사용 중인 앱을 숨길 수 있습니다.)

스팀이나 자주 쓰는 백그라운드 앱은 블랙리스트에 **'열린 윈도우가 없을 때' AltTab 프로그램에서 숨기도록** 설정했습니다.

---

# 마우스 스크롤
![[imgs/mac_setting/mos.png]]
>https://mos.caldis.me/

맥에서 마우스 스크롤이 정말 답답했습니다.
휠을 많이 돌려야 그제서야 스크롤이 되는 느낌이었습니다. 이 프로그램을 쓰니 그제서야 제가 알던 마우스처럼 휠이 작동하더군요. 설정도 다양하게 할 수 있어서 좋습니다.
/imglist open
![[imgs/mac_setting/mos_setting_0.png]]
![[imgs/mac_setting/mos_setting_1.png]]
/imglist close
**스크롤 스텝, 속도, 지속 시간**도 설정할 수 있습니다. **프로그램마다 다르게** 설정할 수도 있습니다. 저는 이미지를 확대 축소할 때 스크롤 스텝이 크게 동작하는 걸 방지하려고 에이스프라이트를 예외 리스트에 넣어놨습니다.

---

# 캡처 도구
![[imgs/mac_setting/shottr.png]]
>https://shottr.cc/

어디 괜찮은 캡처 도구가 없나 싶어서 찾은 앱입니다.
![[imgs/mac_setting/shottr_setting.png]]
캡처한 후에 어떤 동작을 할 건지도 설정할 수 있습니다. 단축키 설정도 가능하고요. 간단하고 편한 것 같습니다.

---

# Yoink
![[imgs/mac_setting/yoink.png]]
>(App Store에서 받을 수 있습니다.)

맥에서 파일 이동이 요상하게 불편하더군요. 드래그 앤 드랍으로 편하게 작업할 수 있게 도와준다고 합니다.

![[imgs/mac_setting/yoink_screenshot.png]]

파일이나 폴더를 잡고 화면 끝부분에 가져가면 저렇게 임시 저장소가 나옵니다. 아무거나 막 집어넣고 꺼내오기 좋은 것 같습니다.

---

# 사진, 비디오, 오디오
일단.. 사진은 [qview](https://interversehq.com/qview/), 비디오와 오디오는 [IINA](https://iina.io/)를 쓰고 있습니다. 근데 이미지 크기에 맞춰서 창 크기가 변하는 느낌이라 익숙해져야 할 것 같습니다.

---

# 구버전 Wacom 드라이버
>https://github.com/thenickdude/wacom-driver-fix

CTL-460 모델을 사용하고 있는데 너무 오래된 제품이라.. 공식 홈페이지에서 배포하는 드라이버를 설치해도 동기화 오류를 내면서 타블렛을 인식하지 못하더라고요. 그래서 찾은 드라이버입니다. (놀랍게도 6년 전부터 계속 유지보수를 하고 있는 것 같습니다. 정말 감사합니다..!)

---

# wine 세팅
윈도우에서 쓰던 작업툴이 맥에는 없어서 wine을 설치했습니다. 제가 만든 게임을 테스트 해봤는데 잘 돌아가더라고요. 작업툴을 돌리는 데에는 실패했지만요. 그래도 그냥 메모해둔다는 느낌으로 적어두겠습니다.

## wine@devel, winetricks

```powershell
brew install cask
brew install --cask --no-quarantine wine@devel
brew install winetricks
```

먼저 `wine@devel`하고 `winetricks`를 설치합니다.

```powershell
softwareupdate --install-rosetta --agree-to-license
```

Apple Silicon이 탑재된 Mac에서는 Rosetta 2를 설치하라는 문구가 뜹니다. 인텔맥 전용으로 제작된 앱을 사용할 수 있게 해준다네요. 혹시 설치하라는 문구가 뜬다면 위 명령어를 수행하면 됩니다.

## dxmt
>https://github.com/3Shain/dxmt

3D 애플리케이션을 실행할 수 있게 해주는 Direct3D 10 및 11를 위한 Metal 기반 레이어... 라고 합니다.
여기서 release 파일을 받아 압축을 풀어줍니다. 그 다음에 파일이 있는 폴더에서 터미널을 연 후에 아래 명령어를 수행합니다. 직접 파일을 옮겨도 됩니다.

```powershell
WINEINSTALL="/Applications/Wine Devel.app/Contents/Resources/wine"
WINEPREFIX="$HOME/.wine"

cp -p ./x86_64-unix/winemetal.so "$WINEINSTALL/lib/wine/x86_64-unix/winemetal.so"
cp -p ./x86_64-windows/winemetal.dll "$WINEINSTALL/lib/wine/x86_64-windows/winemetal.dll"
cp -p ./x86_64-windows/winemetal.dll "$WINEPREFIX/drive_c/windows/system32/winemetal.dll"
cp -p ./x86_64-windows/d3d11.dll "$WINEINSTALL/lib/wine/x86_64-windows/d3d11.dll"
cp -p ./x86_64-windows/dxgi.dll "$WINEINSTALL/lib/wine/x86_64-windows/dxgi.dll"
cp -p ./x86_64-windows/d3d10core.dll "$WINEINSTALL/lib/wine/x86_64-windows/d3d10core.dll"
cp -p ./i386-windows/* "$WINEINSTALL/lib/wine/i386-windows"
```

마지막으로 돌리고자 하는 exe파일을 wine으로 돌려주면 됩니다.

```powershell
wine /Users/[user_name]/Downloads/executeme.exe
```

![[imgs/mac_setting/textlessgame.png]]

제가 예전에 만들었던 게임을 돌려봤습니다. 가벼운 게임정도는 잘 돌리는 것 같습니다.